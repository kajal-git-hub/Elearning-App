<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <ImageView
        android:id="@+id/iv_BgSelect"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:src="@drawable/red_bg_select"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent"/>

    <com.google.android.material.textview.MaterialTextView
        android:id="@+id/et_textInside"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="1"
        android:layout_marginBottom="@dimen/_6dp"
        android:textSize="@dimen/_12sp"
        android:textColor="@color/white"
        android:fontFamily="@font/nunito"
        app:layout_constraintStart_toStartOf="@+id/iv_BgSelect"
        app:layout_constraintEnd_toEndOf="@+id/iv_BgSelect"
        app:layout_constraintTop_toTopOf="@+id/iv_BgSelect"
        app:layout_constraintBottom_toBottomOf="@+id/iv_BgSelect"/>


</androidx.constraintlayout.widget.ConstraintLayout>